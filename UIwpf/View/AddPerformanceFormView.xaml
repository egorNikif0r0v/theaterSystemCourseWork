<UserControl x:Class="UIwpf.View.AddPerformanceFormView_new"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UIwpf.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Добавление нового представления" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="name performance"/>
            <TextBox Text="{Binding NamePerformance}"/>
            <TextBlock Text="totalplace"/>
            <TextBox Text="{Binding TotalPlace}"/>
            <TextBlock Text="beginningDate"/>
            <TextBox Text="{Binding BeginningDate}"/>
            <TextBlock Text="condition"/>
            <TextBox Text="{Binding Condition_}"/>
            <Button Content="К группе" Command="{Binding ToGroup}"/>
        </StackPanel>
    </Grid>
</UserControl>
