<UserControl x:Class="UIwpf.View.PlaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UIwpf.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Информация о местах" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="Number" FontWeight="Bold"/>
            <TextBlock Text="{Binding Number}"/>
            <TextBlock Text="Condition" FontWeight="Bold"/>
            <TextBlock Text="{Binding Condition_}"/>
            <TextBlock Text="Position" FontWeight="Bold"/>
            <TextBlock Text="{Binding Position_}"/>
            <TextBlock Text="Cost" FontWeight="Bold"/>
            <TextBlock Text="{Binding Cost}"/>
            <Button Content="К представлениям" Command="{Binding ToPerformance}"/>
        </StackPanel>
    </Grid>
</UserControl>
